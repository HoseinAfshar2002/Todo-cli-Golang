/*
📁 پروژه Todo CLI با معماری لایه‌ای در Go

🏗️ ساختار معماری:
-------------------
۱. لایه Presentation (نمایش) - main.go
   • دریافت دستورات از کاربر در محیط CLI
   • نمایش خروجی‌ها و پیام‌ها
   • مدیریت state برنامه (authUser)

۲. لایه Business Logic (منطق کسب‌وکار) - main.go
   • اعتبارسنجی دستورات کاربر
   • اجرای منطق register/login/createTask/...
   • مدیریت جریان برنامه

۳. لایه Contract (قرارداد) - contract.go  
   • تعریف interfaceهای UserWriteStore و UserReadStore
   • ایجاد قرارداد بین لایه‌ها
   • امکان جایگزینی Implementation

۴. لایه Implementation (پیاده‌سازی) - filestore.go
   • پیاده‌سازی ذخیره‌سازی در فایل
   • تبدیل JSON ↔ Struct
   • مدیریت خطاهای I/O

🔄 جریان داده:
---------------
کاربر → دستور CLI → main.go → 
      ↓
interface (قرارداد) → filestore.go → 
      ↓
فایل user.txt (ذخیره‌سازی)

🔐 امنیت:
---------
• هش کردن پسورد با MD5 (آموزشی - در تولید از bcrypt استفاده شود)
• بررسی مالکیت داده‌ها (هر کاربر فقط به داده‌های خود دسترسی دارد)

🎯 اهداف آموزشی:
----------------
۱. آشنایی با معماری لایه‌ای در Go
۲. کار با interface برای جداسازی نگرانی‌ها
۳. مدیریت state در برنامه‌های CLI
۴. کار با فایل‌ها و فرمت JSON
۵. پیاده‌سازی الگوی Repository

⚠️ محدودیت‌ها (به عمد برای سادگی):
---------------------------------
۱. ذخیره‌سازی فقط در حافظه برای tasks/categories
۲. عدم استفاده از دیتابیس واقعی
۳. اعتبارسنجی محدود ورودی‌ها
۴. عدم پشتیبانی از همزمانی

🚀 مسیر توسعه:
--------------
۱. جایگزینی filestore با دیتابیس (PostgreSQL)
۲. اضافه شدن REST API
۳. افزودن تست‌های واحد
۴. Dockerization
۵. افزودن features جدید (جستجو، فیلتر، sort)

📚 تکنولوژی‌ها:
---------------
• Go (زبان اصلی)
• JSON (فرمت ذخیره‌سازی)
• MD5 (هش کردن - آموزشی)
• bufio/flag/os (پکیج‌های استاندارد)

👥 نقش‌ها:
----------
• entity.User: مدل داده کاربر
• contract: قراردادهای ذخیره‌سازی  
• filestore.FileStore: پیاده‌سازی فعلی
• main: کنترلر اصلی برنامه

این پروژه یک نمونه آموزشی از ساخت برنامه‌های CLI با معماری تمیز در Go است.
*/
